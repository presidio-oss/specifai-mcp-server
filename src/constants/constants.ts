/**
 * Error message for when no project path is available
 */
export const NO_PROJECT_PATH_ERROR =
  'No project path available. Please provide a project path via: 1) Command line argument (--path /path/to/project), 2) .specifai-path file in working directory, or 3) Use set-project-path tool with a valid cwd parameter.'
